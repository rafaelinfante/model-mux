package net.rafaelinfante.modelmux.provider.model;

/** Cost/capability tier a model sits in. Cost-policy routing picks a tier from prompt complexity. */
public enum ModelTier {
    CHEAP,
    PREMIUM
}
